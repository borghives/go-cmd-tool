{
    "configurations": [
        {
            "name": "sitedb user list",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/sitedb",
            "cwd": "${workspaceFolder}",
            "args": [
                "user",
                "list"
            ]
        }
    ]
}